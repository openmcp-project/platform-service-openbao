apiVersion: openbao.open-control-plane.io/v1alpha1
kind: ControlPlaneEntity
metadata:
  name: eso-reader
  namespace: default
spec:
  controlPlaneTrustRef:
    name: prod
  serviceAccountRef:
    name: external-secrets
    namespace: external-secrets
