apiVersion: openbao.open-control-plane.io/v1alpha1
kind: ControlPlaneEntity
metadata:
  labels:
    app.kubernetes.io/name: platform-service-openbao
    app.kubernetes.io/managed-by: kustomize
  name: eso-reader
  namespace: project-team-a--ws-prod
spec:
  # ControlPlaneTrust in the same namespace. The trust identifies the target
  # ControlPlane, audience, and OpenBao backend context.
  controlPlaneTrustRef:
    name: prod
  # Existing ServiceAccount inside the ControlPlane. NOT created or
  # managed by this service; users own its lifecycle.
  serviceAccountRef:
    name: external-secrets
    namespace: external-secrets
